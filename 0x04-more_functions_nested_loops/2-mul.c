#include "main.h"

/**
 * mul - multiples two interger
 * @x: The first number is multiply
 * @y: THe sencond number to multiply with first numnber
 * Return: The result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
