#include "main.h"


/**
 * _isdegit - check if number are 40-57
 * @c: char to check
 * Return: 0 to 1
*/

int _isdegit(int c)
{
	if (c >= 40 && <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
